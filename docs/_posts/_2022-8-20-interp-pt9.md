---
title: "Interp - an Interpreted Programming Language Pt.9 - While"
author: "Samuel Kriikkula"
tags: interp
---
Hello! This time in Pt. 9 we implement looping.

## Parsing

### LineTypes
Add **LineType**s for while, break, and continue

*src/parser/line.rs:*
```rust
pub enum LineType {

    ...

    If(Expr),
    ElseIf(Expr),
    Else,
    EndScope,
    While(Expr),
    NamedWhile(Expr),
    Break,
    Continue,
}
```

### Grammar
Add the tokens and grammar rules.

```rust

...

match {

    ...
    
    "while",
    "brk",
    "cnt",
    ":",
}

...

pub Line: Line = {
    
    ...

    <l:@L> "while" <a:expr> "{" => Line { type_: LineType::While(a), loc: Loc { filename: filename.to_owned(), line_nr, index: l } },
    <l:@L> "brk" => Line { type_: LineType::Break, loc: Loc { filename: filename.to_owned(), line_nr, index: l } },
    <l:@L> "cnt" => Line { type_: LineType::Continue, loc: Loc { filename: filename.to_owned(), line_nr, index: l } },
}

...

```

## VM

### VM State
Add **While** to **ScopeState**.

*src/vm/mod.rs:*
```rust
```